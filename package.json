{
  "name": "portfolio",
  "private": true,
  "scripts": {
    "dev": "npm --prefix react-app run dev",
    "build": "npm --prefix react-app run build",
    "preview": "npm --prefix react-app run preview",
    "lint": "npm --prefix react-app run lint",
    "storage:list": "node scripts/list-firebase-storage-urls.mjs"
  },
  "devDependencies": {
    "firebase-admin": "^13.6.0"
  }
}
